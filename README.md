# **Курс: Python 3 для PHP и JS-разработчиков 🚀**

[![Open in GitHub Codespaces](https://github.com/codespaces/badge.svg)](https://github.com/codespaces/new?hide_repo_select=true&ref=main&repo=ИМЯ_ПОЛЬЗОВАТЕЛЯ/ИМЯ_РЕПОЗИТОРИЯ)
*(Примечание: ссылка выше будет работать после того, как проект будет загружен на GitHub. Замени `ИМЯ_ПОЛЬЗОВАТЕЛЯ/ИМЯ_РЕПОЗИТОРИЯ` на актуальные данные вашего репозитория.)*

Добро пожаловать! Этот интерактивный курс создан, чтобы помочь веб-разработчикам с опытом в PHP и JavaScript быстро освоить Python, проводя параллели с уже знакомыми концепциями.

---

### **Как начать?**

У вас есть два пути: простой и быстрый (рекомендуется) и классический локальный.

#### **Способ 1: GitHub Codespaces (Рекомендуется ✨)**

Это самый простой способ. Вам не нужно ничего устанавливать на свой компьютер!

1.  Нажмите на кнопку **"Open in GitHub Codespaces"** выше.
2.  Подождите 1-2 минуты, пока GitHub создаст для вас полностью настроенное окружение в облаке. Все зависимости установятся автоматически.
3.  Готово! Можете сразу переходить к разделу **"Ваш первый урок"**.

#### **Способ 2: Локальная настройка**

Если вы предпочитаете работать на своем компьютере.

##### **Шаг 1: Требования к системе**

1.  **Python 3.8+**. Убедитесь, что Python установлен. Проверить можно командой `python3 --version` (на Linux/macOS) или `python --version` (на Windows).
2.  **Visual Studio Code**. [Скачать здесь](https://code.visualstudio.com/).
3.  **Расширение Python для VS Code**. Оно необходимо для работы с Jupyter Notebooks (`.ipynb` файлами).
    *   Откройте VS Code, перейдите во вкладку "Extensions" (Ctrl+Shift+X).
    *   Найдите `Python` от Microsoft и нажмите "Install".
    *   Также установите расширение `Jupyter` от Microsoft.

##### **Шаг 2: Настройка проекта (аналог `npm install` / `composer install`)**

В Python принято изолировать зависимости проекта в **виртуальном окружении (`venv`)**. Это как папка `node_modules` или `vendor`, но для Python.

**Важно для пользователей Linux (Ubuntu/Debian):** В последних версиях `pip` запрещает глобальную установку пакетов, чтобы избежать конфликтов с системными библиотеками. Поэтому создание виртуального окружения — это **обязательный и правильный** шаг.

**Для Linux / macOS:**
```bash
# 1. Создаем виртуальное окружение в папке .venv
# Это как 'npm init' или 'composer init' для создания локальной среды
python3 -m venv .venv

# 2. Активируем его. Теперь все команды pip будут работать только для этого проекта.
source .venv/bin/activate

# 3. Устанавливаем все зависимости из файла requirements.txt
# Это полный аналог 'npm install' или 'composer install'
pip install -r requirements.txt
```

**Для Windows (в терминале `cmd` или `PowerShell`):**
```powershell
# 1. Создаем виртуальное окружение в папке .venv
python -m venv .venv

# 2. Активируем его
.venv\Scripts\activate

# 3. Устанавливаем все зависимости
pip install -r requirements.txt
```

> **💡 Запомните эту последовательность!** Она стандартна для любого Python-проекта.

---

### **Ваш первый урок**

1.  Откройте папку с проектом в VS Code.
2.  Если вы настраивали проект локально, VS Code может предложить выбрать интерпретатор Python. Нажмите на него и выберите тот, который находится в папке `.venv`.
3.  Откройте первый урок: `Python_for_PHP_JS_Devs/Module_1_Introduction/01_Introduction.ipynb`.
4.  Следуйте инструкциям в ноутбуке. Удачи!
